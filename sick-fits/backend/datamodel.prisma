type User {
  id: ID! @unique
  name: String!
  email: String @unique
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  imageLarge: String
  price: Int!
  # createdAt: DateTime!
  # updatedAt: DateTime!
  # user: User!
}
